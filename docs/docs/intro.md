---
sidebar_position: 1
slug: /
title: Introduction
---

# Introduction

`dns-updater` is a lightweight dynamic DNS updater. It detects your current public IP address and ensures one or more DNS records are updated to match it.

It supports multiple DNS providers (including **Google Cloud DNS** and **DigitalOcean DNS**) and is configured using a simple `config.yaml`.
